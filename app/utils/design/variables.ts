import { Icons } from "~/assets/generated/fonts/icons";

/**
 * An array containing all available icon names from the NcIcons set.
 * This constant is used to provide a complete list of icon identifiers
 * for use in components, controls, and documentation (e.g., Storybook).
 */
export const ALL_ICONS = Object.values(Icons).sort();
